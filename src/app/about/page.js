import HeroSection from '../components/HeroSection';

const page = () => {
    return (
        <>
            <HeroSection title={"OUR STORY"} imageURL={"/about1.svg"} />
        </>
    );
};

export default page;
